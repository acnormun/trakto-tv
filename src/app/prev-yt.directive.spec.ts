import { PrevYtDirective } from './prev-yt.directive';

describe('PrevYtDirective', () => {
  it('should create an instance', () => {
    const directive = new PrevYtDirective();
    expect(directive).toBeTruthy();
  });
});

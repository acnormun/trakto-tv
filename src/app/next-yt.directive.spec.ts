import { NextYtDirective } from './next-yt.directive';

describe('NextYtDirective', () => {
  it('should create an instance', () => {
    const directive = new NextYtDirective();
    expect(directive).toBeTruthy();
  });
});
